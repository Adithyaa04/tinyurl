# tinyurl
A tiny desktop app that turns long ugly URLs into short ones. thought it was fun and whimsy so i built it. It even copies the short link to your clipboard.

## Running it
 
```bash
pip install pyperclip pyshorteners
python main.py
```
 
Needs internet since it calls TinyURL's API to actually shorten the link.
